# pipewire-pulse.conf.d

`custom-modules.conf` lives here on each machine but stays gitignored, like
`hypr/monitors.lua`: it references machine-specific hardware (the USB mic
virtmic chain). This README only keeps the directory tracked so the
`~/.config/pipewire` symlink works on a fresh checkout.
