# Sourced by the uwsm/Hyprland session, not by zsh, so only session-wide
# environment belongs here. Interactive shell config lives in zsh/.zshrc.
export EDITOR='nvim'
export VISUAL=$EDITOR
export PATH="$HOME/.local/bin:$PATH"
