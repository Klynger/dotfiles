# Tools the tmux config depends on, checked by scripts/check_requirements.sh
# before symlinking.
#
# tool | minimum version (empty = presence only) | how to install
tmux | 3.3.0 | brew install tmux
git | | brew install git
