# macOS-only symlinks (source:target)
# kitty: config uses cmd+ bindings and macos_option_as_alt
$(pwd)/kitty:$HOME/.config/kitty
# wezterm: on Linux ~/.config/wezterm comes from hyprland-desktop-config
$(pwd)/wezterm/wezterm.lua:$HOME/.wezterm.lua
