# Tools the nvim config depends on, checked by scripts/check_requirements.sh
# before symlinking and by :checkhealth core inside Neovim.
#
# tool | minimum version (empty = presence only) | how to install
nvim | 0.12.0 | brew install neovim
tree-sitter | 0.26.1 | brew install tree-sitter-cli
cc | | xcode-select --install
git | | brew install git
curl | | brew install curl
tar | | ships with macOS
node | 20.0.0 | brew install node
rg | | brew install ripgrep
shfmt | | brew install shfmt
